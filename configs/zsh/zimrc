# prompt
zmodule prompt-pwd
zmodule git-info

# aliases, colours
zmodule utility

# correct key behaviour
zmodule input

# local additions
zmodule ~/.dotfiles/configs/zsh -s prompt.zsh-theme \
    -s opts.zsh \
    -s completion.zsh \
    -s keys.zsh \
    -s prompt_config.zsh \
    -s aliases.zsh \
    -s skim.zsh

# completions
zmodule zsh-users/zsh-completions --fpath src
zmodule completion
# needs hacking (to run late; in zshrc): https://github.com/BronzeDeer/zsh-completion-sync/issues/1
# -z -- frozen
zmodule BronzeDeer/zsh-completion-sync -d --frozen

zmodule zsh-users/zsh-syntax-highlighting
zmodule zsh-users/zsh-autosuggestions
