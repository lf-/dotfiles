wallpaper.png
qt4/Trolltech.conf
gtk3/bookmarks
fonts/.uuid
*.zwc
buck-out/

# Test runs write these into the source tree, deliberately: `.hypothesis` holds
# the failing examples hypothesis replays. (Breaks under remote execution;
# `-p no:cacheprovider` if that lands.)
.pytest_cache/
.hypothesis/
