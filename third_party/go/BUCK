# `go` comes from `buck2 run toolchains//:go[go]`, invoked by the script itself:
# a toolchain rule cannot be reached through `$(exe ...)`. Hence the $GO override.
python_bootstrap_binary(
    name = "buckify",
    main = "buckify.py",
)
